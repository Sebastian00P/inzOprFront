export const BaseUrl = "https://localhost:5001"

enum Endpoint {
    // TODO add endpoints
    USERS_ENDPOINT = "/api/User",
    COMMENTS_ENDPOINT = "/api/Comments",
    ADS_ENDPOINT = "/api/Ads",
}

export default Endpoint